{
  "name": "physique-meter",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered physique analysis and progress tracking platform.",
  "main": "backend/server.js",
  "scripts": {
    "web": "npm --prefix apps/web run dev",
    "api": "python -m uvicorn apps.api.app.main:app --reload --port 8000",
    "start": "node backend/server.js",
    "serve": "node backend/server.js",
    "check": "node -e \"const { calculatePhysique } = require('./shared/physique'); const result = calculatePhysique({ sex: 'male', heightCm: 178, weightKg: 82, goalBodyFat: 14 }); if (!Number.isFinite(result.bodyFat)) process.exit(1); console.log('check ok');\""
  },
  "keywords": [
    "fitness",
    "ai",
    "nextjs",
    "fastapi",
    "mediapipe",
    "physique",
    "body-composition",
    "github-pages"
  ],
  "license": "MIT"
}
